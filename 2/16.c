// Write a program to print a histogram of the lengths of words in its
// input.


#include <stdio.h>
main()
{
	int c;
	int word[10];

	for (i = 0; i < 10; ++i)
		word[i] = 0;

	while ((c = getchar()) != EOF) {
}
